<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Social Media : home page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
    <script src="https://cdn.jsdelivr.net/npm/axios@1.6.2/dist/axios.min.js" integrity="sha256-VQ8m0Dd2xi0z6QuAKMa04ufRMBxv92nP+UWSqT33HGg=" crossorigin="anonymous"></script>

    <div class="head  d-flex  justify-content-between px-5 mx-1 py-2
       " style="width: 100%;">
        <h1 class=" my-2 text-primary fs-4 ">Home page </h1>

        <div class="mydets  px-4 py-2 d-flex gap-2">
            <a href="/logout" class="px-3 py-1 " style="text-decoration: none; background-color : royalblue;  color:white; border-radius: 50px;">Log out</a>
            <a id="profile" href="/open/profile/<%= userloggedin.username %> ">
                <div class="profile">
                    <img src="/images/uploads/<%= userloggedin.profile %>" alt="retry">
                </div>
            </a>

        </div>


    </div>





    <div class="allposts  d-flex  flex-wrap px-4 ">

        <% allposts.reverse().forEach(function(post){ %>

            <div class="card mx-4 my-4" style="width: 19rem;">

                <div class="userdets d-flex align-items-center gap-2 my-2 justify-content-between px-3">
                    <div class="left d-flex gap-1 align-items-center">
                        <img id="userprofile" src="/images/uploads/<%= post.user.profile %>" alt="">
                        <a href="/profile/<%= post.user.username %>" class="text-decoration-none text-dark">
                            <%= post.user.username %>
                        </a>
                    </div>

                    <% if(post.user.followers.indexOf(userloggedin._id) === -1){ %>
                        <a href="/follow/<%= post.user._id %>" class=" text-decoration-none">Follow</a>
                        <%   }  else{   %>
                            <a href="/follow/<%= post.user._id %>" class=" text-decoration-none">Following</a>
                            <%   }   %>


                                <!-- <a href="/follow/<%= post.user._id %>" class=" text-decoration-none">following</a> -->

                </div>


                <img src="/images/uploads/<%= post.postimage %>" class="card-img-top" alt="retry ">
                <div class="card-body">

                    <div class="like d-flex  gap-4 justify-content-between ">
                        <div class="like d-flex align-items-center gap-1  " style="white-space: nowrap;">
                            <p class="my-2 fs-5">
                                <%= post.likes.length %>
                            </p>
                            <% if(post.likes.indexOf(userloggedin._id) === -1){ %>
                                <a type="submit" href="/like/<%= post._id %>" class="btn text-primary fs-5 ">like</a>
                                <%  }  else{ %>
                                    <a type="submit" href="/like/<%= post._id %>" class="btn text-primary fs-5 ">unlike</a>
                                    <% }  %>
                        </div>

                        <!-- <div class="comment d-flex align-items-center gap-1 " style="white-space: nowrap;">
                            <p class="my-2 fs-5">0</p>
                          <a  class="btn text-primary fs-5 my-2 ">
                              view comments
                          </a>
                      
                        </div> -->

                        <% if(userloggedin.savedposts.indexOf(post._id) === -1){ %>

                            <a style="text-decoration: none;" id="save" href="/save/<%= post._id %>" class="mt-3   rounded-2xl px-2">save</a>

                            <% } else { %>
                                <a style="text-decoration: none;" id="unsave" href="/unsave/<%= post._id %>" class="mt-3  rounded-2xl px-2">saved</a>

                                <% }   %>



                    </div <p class="card-text my-5">
                    <%=post.postdata %>
                        </p>
                        <p class="my-4">
                            <%= post.date.toDateString() %>
                        </p>
                </div>

            </div>

            <% })  %>

    </div>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "gilroy";
        }
        
        html,
        body {
            width: 100%;
            height: 100%;
        }
        
         ::-webkit-scrollbar {
            width: 13px;
            background-color: whitesmoke;
        }
        
         ::-webkit-scrollbar-thumb {
            background-color: royalblue;
            width: 20px;
        }
        
        h1 {
            text-transform: capitalize;
        }
        
        .allposts {
            position: relative;
            width: 100%;
            overflow-x: hidden;
            overflow-y: auto;
        }
        
        .postcreate {
            height: 5vmax;
            display: flex;
            align-items: center;
        }
        
        #userprofile {
            width: 35px;
            height: 35px;
            border-radius: 50%;
        }
        
        #userprofile img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
        }
        
        .profile {
            width: 30px;
            height: 30px;
            background-color: red;
            border-radius: 50%;
        }
        
        .profile img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }
        
        #profile {
            position: relative;
            margin: right 5vmax;
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>

</html>